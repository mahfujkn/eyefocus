<UserControl x:Class="EyeFocus.UI.Views.MonitorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="680"
             FontFamily="Segoe UI"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             RenderOptions.ClearTypeHint="Enabled">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <!-- Content Container (20px horizontal padding, 12px top, 20px bottom) -->
        <StackPanel Margin="20,12,20,20" HorizontalAlignment="Stretch">

            <!-- 1. Header -->
            <Grid Margin="0,0,0,12" HorizontalAlignment="Stretch">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Monitors" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource OnSurfaceBrush}" />
                    <TextBlock Text="Detected displays and hardware control capabilities." FontSize="12" Foreground="{DynamicResource OutlineBrush}" Margin="0,2,0,0" />
                </StackPanel>

                <Button Style="{StaticResource M3TonalButtonStyle}" Command="{Binding RefreshMonitorsCommand}" HorizontalAlignment="Right" Padding="12,5" Height="28" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="{StaticResource IconRefresh}" Fill="{DynamicResource OnSecondaryContainerBrush}" Width="10" Height="10" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="Detect Displays" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <!-- 2. Monitor Rows List -->
            <ItemsControl ItemsSource="{Binding Monitors}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource M3CardStyle}" Margin="0,0,0,10" HorizontalAlignment="Stretch" Padding="14,12">
                            <StackPanel>
                                <!-- Title, Primary Badge, & Resolution -->
                                <Grid Margin="0,0,0,6">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FriendlyName}" FontWeight="Bold" FontSize="13.5" Foreground="{DynamicResource OnSurfaceBrush}" VerticalAlignment="Center" />
                                        <Border Background="{DynamicResource PrimaryContainerBrush}" CornerRadius="4" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="PRIMARY" FontWeight="Bold" FontSize="8.5" Foreground="{DynamicResource PrimaryBrush}" />
                                        </Border>
                                    </StackPanel>

                                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource OutlineBrush}">
                                        <Run Text="{Binding Width}" /> × <Run Text="{Binding Height}" /> · <Run Text="{Binding DpiScaleX, StringFormat='{}{0:P0}'}" /> DPI
                                    </TextBlock>
                                </Grid>

                                <TextBlock Text="{Binding DeviceName, StringFormat='Device: {0}'}" FontSize="10.5" Foreground="{DynamicResource OutlineBrush}" Margin="0,0,0,8" />

                                <!-- Capabilities Information Strip -->
                                <Border Background="{DynamicResource SurfaceContainerHighBrush}" BorderBrush="{DynamicResource OutlineVariantBrush}" BorderThickness="1" CornerRadius="6" Padding="10,6" Margin="0,0,0,2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <!-- DDC/CI -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="DDC/CI Brightness" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource OutlineBrush}" />
                                            <TextBlock Text="{Binding SupportsDdcCi, Converter={x:Null}}" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" Margin="0,1,0,0" />
                                        </StackPanel>

                                        <!-- Hardware Color -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Hardware Color" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource OutlineBrush}" />
                                            <TextBlock Text="{Binding SupportsHardwareBrightness, Converter={x:Null}}" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" Margin="0,1,0,0" />
                                        </StackPanel>

                                        <!-- Gamma -->
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Windows Gamma" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource OutlineBrush}" />
                                            <TextBlock Text="Ready ✓" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" Margin="0,1,0,0" />
                                        </StackPanel>

                                        <!-- Software Dimming -->
                                        <StackPanel Grid.Column="3">
                                            <TextBlock Text="Software Dim" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource OutlineBrush}" />
                                            <TextBlock Text="Ready ✓" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" Margin="0,1,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
